# HTML Tables Practice

This repository contains a simple HTML project demonstrating how to create and structure **tables in HTML**. It focuses on using table rows, headers, data cells, and captions correctly.

---

## 📌 Project Overview

The goal of this project is to practice:

- Creating tables using `<table>`
- Defining rows with `<tr>`
- Adding header cells with `<th>`
- Adding data cells with `<td>`
- Using `<caption>` to describe a table

Tables are commonly used to display structured data such as schedules, lists, and records.

---

## 📂 Project Structure


